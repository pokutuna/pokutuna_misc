import scala.io._

def toInt(in: String): Option[Int] =
  try {
    Some(Integer.parseInt(in.trim))
  } catch {
    
    case e: Exception => 
      println(\"ERROR: \" + e) // TODO: handle exception\n}
  }


